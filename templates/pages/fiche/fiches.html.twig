{% extends 'base.html.twig' %}

{%  block body %}
    <div class="container mt-4">
        <h1>Gérer les fiches</h1>

        <table class="table table-striped">
            <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for fiche in fiches %}
                <tr>
                    <td>{{ fiche.id }}</td>
                    <td><a href="{{ path('fiche', {id: fiche.id}) }}">{{ fiche.title }}</a></td>
                    <td>
                        <a href="{{ path('fiche.edit', {id: fiche.id}) }}" class="btn btn-secondary">Éditer</a>
                        <form method="post" action="{{ path('fiche.delete', {id: fiche.id}) }}" style="display: inline-block">
                            <input type="hidden" name="_method" value="DELETE">
                            <button class="btn btn-danger">Supprimer</button>
                        </form>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <div class="text-right">
            <a href="{{ path('fiches.new') }}" class="btn btn-primary">Créer une fiche</a>
        </div>
    </div>
{% endblock %}