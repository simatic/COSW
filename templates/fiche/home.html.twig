{% extends 'base.html.twig' %}
{% block body %}
<h1>Bienvenue</h1>
<section class="soutenance">

<article>
	{% for rubrique in rubriques %}
	<div class="jumbotron">
	
	<h2 class="display-3">{{ rubrique.nom }}</h2>
	<div class="content" style="background-color: #002b36;">	
	
	<ul class="list-group">
	{% for item in rubrique.items %}
  <li class="list-group-item list-group-item-action active">
    {{ item.nom }} ({{ item.note }} /20)
<a href="#" data-id="{{ item.id }}">
<div class="progress" style="background-color: #002b36;">
  <div class="progress-bar progress-bar-striped progress-bar-animated" role="progressbar" aria-valuenow="75" aria-valuemin="0" aria-valuemax="100"  style="width: {{ item.note/20 *100 }}%;" >
</div>
</div>
<button type="button" class="btn btn-danger">Supprimer</button></td></a>
  </li>
    {% endfor %}
    
    </ul>
    </div>
        </div>
	{% endfor %}
			
		
</article>
</section>
{% endblock %}
