{% extends 'base.html.twig' %}

{% block main %}
    <h1>AccountRequest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Identifiant</th>
                <td>{{ account_request.id }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ account_request.firstName }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ account_request.lastName }}</td>
            </tr>
            <tr>
                <th>Adresse mmail</th>
                <td>{{ account_request.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('account_request_index') }}">back to list</a>

    <a href="{{ path('account_request_edit', {'id': account_request.id}) }}">edit</a>

    {{ include('account_request/_delete_form.html.twig') }}
{% endblock %}
