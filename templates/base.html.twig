<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
      <link href="https://bootswatch.com/4/solar/bootstrap.min.css" rel="stylesheet">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
           {% block stylesheets %}
           {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}
        
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
          <a class="navbar-brand" href="/session">Application</a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          
          <div class="collapse navbar-collapse" id="navbarColor03">
            <ul class="navbar-nav mr-auto">	
    {% if is_granted('ROLE_ADMIN') %}
    <li style="padding:0 15px;"><a href="{{ path('home') }}">COS</a></li>
        {% if app.user %}
            <li style="padding:0 15px;"><a href="{{ path('logout') }}">Se déconnecter</a></li>
            {% if is_granted('ROLE_ADMIN') %}
                {% set url = 'admin' %}
            {% elseif is_granted('ROLE_CREATOR') %}
                {% set url = 'creator' %}
            {% else %}
                {% set url = 'home' %}
            {% endif %}
            <li style="padding:0 15px;"><a href="{{ path(url) }}">Mon espace</a></li>
        {% else %}
            <li style="padding:0 15px;"><a href="{{ path('login') }}">Se connecter</a></li>
        {% endif %}
    {% endif %}
            </ul>
              {% if app.user %}
                <a class="navbar-brand" href="{{ path("logout") }}">Déconnexion</a>
              {% endif %}
 	</div>
  </nav>
        {% block javascripts %}
        
        {% endblock %}
    </head>
    <body><br>
    <div class="container">
        {% block main %}{% endblock %}
        
        {% block body %}
        {% endblock %}
    </div>
    </body>
</html>
